package com.mercadolivre.payment.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import com.mercadolivre.payment.entity.Consumidor;

public interface ConsumidorRepository extends JpaRepository<Consumidor, Long> {

/*
interface :
Em Java, uma interface é um tipo de classe especial que define uma série de métodos que devem ser implementados por qualquer classe que a implemente. As interfaces são usadas para definir contratos, ou seja, garantir que classes que implementam uma determinada interface possuem certos comportamentos e atributos. As interfaces não possuem implementação e não podem ser instanciadas, somente implementadas.@ByVictorsa-meli 
 

extends:
 Por outro lado, a palavra-chave "extends" é usada para indicar que uma classe está estendendo outra classe. Isso significa que a classe filha herda todos os atributos e métodos da classe pai, além de poder adicionar novos atributos e métodos ou sobrescrever os existentes. A classe filha também pode implementar uma ou mais interfaces.
 
 
*/



}